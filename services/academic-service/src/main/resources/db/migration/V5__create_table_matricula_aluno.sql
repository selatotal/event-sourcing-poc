create table matricula_aluno (
    id_matricula varchar(255) primary key not null,
    codigo_aluno varchar(255),
    periodo_letivo varchar(255),
    curso varchar(255),
    grade varchar(255),
    periodo varchar(255)
);